import 'package:flutter/material.dart';

/// 검색 타겟 옵션
class SearchTargetWidget extends StatelessWidget {
  const SearchTargetWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
